import { BlurMenuDirective } from './blur-menu.directive';

describe('BlurMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new BlurMenuDirective();
    expect(directive).toBeTruthy();
  });
});
